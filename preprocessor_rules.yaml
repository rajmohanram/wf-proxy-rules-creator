global:
- action: allow
  match: cpu.utilization.average.rate.percent
  rule: cpu-utilization-average-rate-percent
  scope: metricName
- action: allow
  match: cpu.usage.user
  rule: cpu-usage-user
  scope: metricName
- action: allow
  match: cpu.usage.system
  rule: cpu-usage-system
  scope: metricName
